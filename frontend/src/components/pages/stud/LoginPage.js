import {FormWrapper, LoginForm} from "components/forms";
import React from "react";

const LoginPage = () => {
    return (
        <FormWrapper>
            <LoginForm />
        </FormWrapper>
    );
};
export default LoginPage;
