import {FormWrapper, LoginForm} from "components/Login";
import React from "react";

const LoginPage = () => {
    return (
        <FormWrapper>
            <LoginForm />
        </FormWrapper>
    );
};
export default LoginPage;
