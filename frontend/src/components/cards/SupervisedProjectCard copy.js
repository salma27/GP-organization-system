import React, { useState } from "react";
import { Badge, Card, Button, OverlayTrigger, Tooltip } from "react-bootstrap";

const SupervisedProjectCard = (props) => {
    return (
        <Card className="mb-3">
            <Card.Body>
                <button>hh</button>
            </Card.Body>
        </Card>
    );
};
export default SupervisedProjectCard;
